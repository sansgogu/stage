<!DOCTYPE html>

<title>Edit Vente</title>

{% block body %}
    <h1>Edit Vente</h1>

    {{ include('vente/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_vente_index') }}">back to list</a>

    {{ include('vente/_delete_form.html.twig') }}
{% endblock %}
