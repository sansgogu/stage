<form method="post" action="{{ path('app_client_pro_delete', {'id': client_pro.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ client_pro.id) }}">
    <button class="btn btn-danger">Delete</button>

</form>
