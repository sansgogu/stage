{% extends "baseadmin.html.twig" %}



{% block body %}


    <!-- Main Content -->
    <main class="container mb-5">
        <div class="row justify-content-center">
            <div class="col-lg-10">
                <div class="card">
                    <div class="card-header py-3">
                        <h5 class="card-title mb-0"><i class="bi bi-plus-circle me-2"></i>Create New Notification</h5>
                    </div>
                    <div class="card-body">
                        <!-- Notification Type Selection -->
                        <div class="form-section">
                            <h5 class="section-title">Notification Type</h5>
                            <div class="notification-type">
                                <div class="type-option active">
                                    <i class="bi bi-chat-dots"></i>
                                    <div>Message</div>
                                </div>
                                <div class="type-option">
                                    <i class="bi bi-exclamation-triangle"></i>
                                    <div>Alert</div>
                                </div>
                                <div class="type-option">
                                    <i class="bi bi-calendar-event"></i>
                                    <div>Reminder</div>
                                </div>
                                <div class="type-option">
                                    <i class="bi bi-megaphone"></i>
                                    <div>Announcement</div>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Form -->
                        {{ include('notification/_form.html.twig') }}
                        
                        <!-- Preview Section -->
                        <div class="form-section">
                            <h5 class="section-title">Notification Preview</h5>
                            <div class="card">
                                <div class="card-body">
                                    <div class="d-flex align-items-center mb-3">
                                        <div class="bg-primary rounded-circle p-2 me-3">
                                            <i class="bi bi-bell text-white"></i>
                                        </div>
                                        <div>
                                            <h6 class="mb-0">Notification Title</h6>
                                            <small class="text-muted">Just now</small>
                                        </div>
                                        <span class="priority-badge priority-high ms-auto">High Priority</span>
                                    </div>
                                    <p class="mb-0">This is a preview of how your notification will appear to users.</p>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Help Text -->
                        <div class="mt-4">
                            <div class="alert alert-info">
                                <h6><i class="bi bi-info-circle me-2"></i>Notification Best Practices</h6>
                                <ul class="mb-0">
                                    <li>Keep messages clear and concise</li>
                                    <li>Use appropriate priority levels</li>
                                    <li>Consider your audience when scheduling</li>
                                    <li>Test notifications before sending to all users</li>
                                </ul>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="bg-light py-4 mt-5">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <p class="mb-0">© 2023 Notification System. All rights reserved.</p>
                </div>
                <div class="col-md-6 text-md-end">
                    <a href="#" class="text-decoration-none text-muted me-3">Privacy Policy</a>
                    <a href="#" class="text-decoration-none text-muted me-3">Terms of Service</a>
                    <a href="#" class="text-decoration-none text-muted">Support</a>
                </div>
            </div>
        </div>
    </footer>

    <!-- Bootstrap & JavaScript Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- Form Enhancement Script -->
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Add required field indicators
            const labels = document.querySelectorAll('label');
            labels.forEach(label => {
                if (label.textContent.includes('Title') || 
                    label.textContent.includes('Message') ||
                    label.textContent.includes('Recipient')) {
                    label.classList.add('required-field');
                }
            });
            
            // Enhance form controls with Bootstrap styling
            const inputs = document.querySelectorAll('input, textarea, select');
            inputs.forEach(input => {
                if (!input.classList.contains('btn')) {
                    input.classList.add('form-control');
                    
                    // Add character counter for message textarea
                    if (input.tagName === 'TEXTAREA' && input.name.includes('message')) {
                        const charCount = document.createElement('div');
                        charCount.className = 'character-count';
                        charCount.textContent = '0 characters';
                        input.parentNode.appendChild(charCount);
                        
                        input.addEventListener('input', function() {
                            charCount.textContent = `${this.value.length} characters`;
                        });
                    }
                }
            });
            
            // Style the submit button
            const submitButton = document.querySelector('button[type="submit"]');
            if (submitButton) {
                submitButton.classList.add('btn', 'btn-primary', 'me-2');
                submitButton.innerHTML = '<i class="bi bi-send me-2"></i>Create Notification';
            }
            
            // Style the back link as a button
            const backLink = document.querySelector('a[href*="app_notification_index"]');
            if (backLink && !backLink.classList.contains('btn')) {
                backLink.classList.add('btn', 'btn-outline-secondary');
                backLink.innerHTML = '<i class="bi bi-arrow-left me-2"></i>Back to List';
            }
            
            // Notification type selection
            const typeOptions = document.querySelectorAll('.type-option');
            typeOptions.forEach(option => {
                option.addEventListener('click', function() {
                    typeOptions.forEach(opt => opt.classList.remove('active'));
                    this.classList.add('active');
                });
            });
            
            // Priority selector enhancement
            const prioritySelect = document.querySelector('select[name*="priority"]');
            if (prioritySelect) {
                prioritySelect.classList.add('form-select');
                
                // Add priority badges to options
                const options = prioritySelect.options;
                for (let i = 0; i < options.length; i++) {
                    const option = options[i];
                    if (option.value === 'high') {
                        option.setAttribute('data-class', 'priority-high');
                    } else if (option.value === 'medium') {
                        option.setAttribute('data-class', 'priority-medium');
                    } else if (option.value === 'low') {
                        option.setAttribute('data-class', 'priority-low');
                    }
                }
            }
        });
    </script>
</body>
</html>
{% endblock %}
