<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Navigation Améliorée - Argon Dashboard</title>
    <!-- Fonts and icons -->
    <link href="https://fonts.googleapis.com/css?family=Open+Sans:300,400,600,700" rel="stylesheet">
    <script src="https://kit.fontawesome.com/42d5adcbca.js" crossorigin="anonymous"></script>
    <style>
        /* Styles améliorés pour la navigation */
        :root {
            --primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            --secondary-gradient: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            --dark-bg: #1a2035;
            --light-text: #ffffff;
            --hover-bg: rgba(255, 255, 255, 0.1);
            --transition-speed: 0.3s;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Open Sans', sans-serif;
            background-color: #f8f9fa;
            color: #333;
        }
        
        .navbar-container {
            max-width: 1400px;
            margin: 20px auto;
            border-radius: 16px;
            overflow: hidden;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        }
        
        .improved-navbar {
            background: var(--primary-gradient);
            padding: 0.75rem 1.5rem;
            position: relative;
            display: flex;
            flex-wrap: wrap;
            align-items: center;
            justify-content: space-between;
            border-radius: 16px;
            transition: all var(--transition-speed) ease;
        }
        
        .nav-content {
            display: flex;
            align-items: center;
            justify-content: space-between;
            width: 100%;
        }
        
        .breadcrumb-section {
            display: flex;
            flex-direction: column;
        }
        
        .breadcrumb {
            display: flex;
            flex-wrap: wrap;
            align-items: center;
            padding: 0;
            margin-bottom: 0.25rem;
        }
        
        .breadcrumb-item {
            display: flex;
            align-items: center;
            color: rgba(255, 255, 255, 0.8);
            font-size: 0.875rem;
        }
        
        .breadcrumb-item a {
            color: rgba(255, 255, 255, 0.8);
            text-decoration: none;
            transition: color var(--transition-speed) ease;
        }
        
        .breadcrumb-item a:hover {
            color: var(--light-text);
        }
        
        .breadcrumb-item + .breadcrumb-item::before {
            content: '/';
            padding: 0 0.5rem;
            color: rgba(255, 255, 255, 0.5);
        }
        
        .page-title {
            font-size: 1.35rem;
            font-weight: 700;
            color: var(--light-text);
            margin: 0;
        }
        
        .nav-controls {
            display: flex;
            align-items: center;
            gap: 1rem;
        }
        
        .search-container {
            position: relative;
            margin-right: 0.5rem;
        }
        
        .search-icon {
            position: absolute;
            left: 12px;
            top: 50%;
            transform: translateY(-50%);
            color: #6c757d;
            z-index: 1;
        }
        
        .search-input {
            padding: 0.6rem 1rem 0.6rem 2.75rem;
            font-size: 0.875rem;
            border: none;
            border-radius: 12px;
            background: rgba(255, 255, 255, 0.9);
            width: 240px;
            transition: all var(--transition-speed) ease;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.05);
        }
        
        .search-input:focus {
            outline: none;
            box-shadow: 0 0 0 3px rgba(255, 255, 255, 0.3);
            background: white;
        }
        
        .nav-actions {
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }
        
        .nav-action-btn {
            display: flex;
            align-items: center;
            justify-content: center;
            background: transparent;
            border: none;
            color: var(--light-text);
            width: 38px;
            height: 38px;
            border-radius: 50%;
            cursor: pointer;
            transition: all var(--transition-speed) ease;
            position: relative;
        }
        
        .nav-action-btn:hover {
            background: var(--hover-bg);
        }
        
        .notification-badge {
            position: absolute;
            top: -5px;
            right: -5px;
            background: var(--secondary-gradient);
            color: white;
            font-size: 0.7rem;
            width: 18px;
            height: 18px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
        }
        
        .user-menu {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            margin-left: 0.5rem;
            padding: 0.35rem 0.75rem;
            border-radius: 20px;
            cursor: pointer;
            transition: all var(--transition-speed) ease;
        }
        
        .user-menu:hover {
            background: var(--hover-bg);
        }
        
        .user-avatar {
            width: 36px;
            height: 36px;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid rgba(255, 255, 255, 0.3);
        }
        
        .user-info {
            display: flex;
            flex-direction: column;
        }
        
        .user-name {
            color: var(--light-text);
            font-size: 0.875rem;
            font-weight: 600;
            line-height: 1.2;
        }
        
        .user-role {
            color: rgba(255, 255, 255, 0.7);
            font-size: 0.75rem;
            line-height: 1.2;
        }
        
        /* Dropdown amélioré */
        .dropdown-menu {
            position: absolute;
            top: 100%;
            right: 0;
            width: 320px;
            background: white;
            border-radius: 12px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
            z-index: 1000;
            padding: 1rem 0;
            margin-top: 0.75rem;
            opacity: 0;
            visibility: hidden;
            transform: translateY(10px);
            transition: all var(--transition-speed) ease;
        }
        
        .dropdown-menu.show {
            opacity: 1;
            visibility: visible;
            transform: translateY(0);
        }
        
        .dropdown-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 1.25rem 0.75rem;
            border-bottom: 1px solid #e9ecef;
        }
        
        .dropdown-title {
            font-size: 1rem;
            font-weight: 600;
            color: #344767;
            margin: 0;
        }
        
        .dropdown-view-all {
            color: #6c757d;
            font-size: 0.875rem;
            text-decoration: none;
        }
        
        .dropdown-view-all:hover {
            color: #000;
        }
        
        .dropdown-item {
            display: flex;
            align-items: center;
            padding: 0.75rem 1.25rem;
            text-decoration: none;
            color: #67748e;
            transition: all 0.2s ease;
            border-left: 3px solid transparent;
        }
        
        .dropdown-item:hover {
            background-color: #f8f9fa;
            border-left: 3px solid #5e72e4;
        }
        
        .dropdown-item-icon {
            width: 40px;
            height: 40px;
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: 1rem;
            flex-shrink: 0;
        }
        
        .icon-blue {
            background: linear-gradient(135deg, #5e72e4 0%, #825ee4 100%);
        }
        
        .icon-red {
            background: linear-gradient(135deg, #f5365c 0%, #f56036 100%);
        }
        
        .icon-green {
            background: linear-gradient(135deg, #2dce89 0%, #2dcecc 100%);
        }
        
        .icon-orange {
            background: linear-gradient(135deg, #fb6340 0%, #fbb140 100%);
        }
        
        .dropdown-item-content {
            flex: 1;
            min-width: 0;
        }
        
        .dropdown-item-title {
            font-size: 0.875rem;
            font-weight: 600;
            color: #344767;
            margin-bottom: 0.25rem;
        }
        
        .dropdown-item-desc {
            font-size: 0.75rem;
            color: #6c757d;
        }
        
        .dropdown-item-time {
            font-size: 0.75rem;
            color: #6c757d;
            display: flex;
            align-items: center;
        }
        
        .dropdown-footer {
            text-align: center;
            padding: 0.75rem 1.25rem 0;
            border-top: 1px solid #e9ecef;
        }
        
        /* Menu mobile */
        .mobile-menu-btn {
            display: none;
            flex-direction: column;
            justify-content: center;
            width: 24px;
            height: 24px;
            cursor: pointer;
        }
        
        .mobile-menu-btn span {
            height: 2px;
            width: 100%;
            background-color: var(--light-text);
            margin: 2px 0;
            transition: all var(--transition-speed) ease;
            border-radius: 2px;
        }
        
        /* Responsive */
        @media (max-width: 992px) {
            .nav-content {
                flex-wrap: wrap;
            }
            
            .search-input {
                width: 180px;
            }
            
            .user-info {
                display: none;
            }
        }
        
        @media (max-width: 768px) {
            .improved-navbar {
                padding: 0.75rem 1rem;
            }
            
            .breadcrumb-section {
                order: 2;
                width: 100%;
                margin-top: 0.75rem;
                padding-top: 0.75rem;
                border-top: 1px solid rgba(255, 255, 255, 0.1);
            }
            
            .nav-controls {
                order: 1;
                width: 100%;
                justify-content: space-between;
            }
            
            .search-container {
                flex: 1;
                margin-right: 0.5rem;
            }
            
            .search-input {
                width: 100%;
            }
            
            .mobile-menu-btn {
                display: flex;
            }
            
            .nav-action-btn span.text {
                display: none;
            }
            
            .user-menu {
                padding: 0.25rem;
            }
        }
        
        /* Animation pour le dropdown */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .dropdown-menu {
            animation: fadeIn 0.3s ease forwards;
        }
    </style>
</head>
<body>
    <div class="navbar-container">
        <nav class="improved-navbar">
            <div class="nav-content">
                <div class="breadcrumb-section">
                    <nav aria-label="breadcrumb">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item"><a href="javascript:;">Pages</a></li>
                            <li class="breadcrumb-item active" aria-current="page">Dashboard</li>
                        </ol>
                    </nav>
                    <h1 class="page-title">Dashboard</h1>
                </div>
                
                <div class="nav-controls">
                    <div class="search-container">
                        <span class="search-icon">
                            <i class="fas fa-search"></i>
                        </span>
                        <input type="text" class="search-input" placeholder="Rechercher..." />
                    </div>
                    
                    <div class="nav-actions">
                        <button class="nav-action-btn" aria-label="Online Builder">
                            <i class="fas fa-palette"></i>
                        </button>
                        
                        <button class="nav-action-btn" aria-label="Sign In">
                            <i class="fas fa-user"></i>
                            <span class="text">Sign In</span>
                        </button>
                        
                        <button class="nav-action-btn" id="settings-btn" aria-label="Settings">
                            <i class="fas fa-cog"></i>
                        </button>
                        
                        <div class="notification-container" style="position: relative;">
                            <button class="nav-action-btn" id="notifications-btn" aria-label="Notifications">
                                <i class="fas fa-bell"></i>
                                <span class="notification-badge">3</span>
                            </button>
                            
                            <div class="dropdown-menu" id="notifications-dropdown">
                                <div class="dropdown-header">
                                    <h3 class="dropdown-title">Notifications</h3>
                                    <a href="#" class="dropdown-view-all">Voir tout</a>
                                </div>
                                
                                <a href="#" class="dropdown-item">
                                    <div class="dropdown-item-icon icon-blue">
                                        <i class="fas fa-envelope text-white"></i>
                                    </div>
                                    <div class="dropdown-item-content">
                                        <h4 class="dropdown-item-title">Nouveau message</h4>
                                        <p class="dropdown-item-desc">Vous avez reçu un message de Laur</p>
                                        <span class="dropdown-item-time">
                                            <i class="fas fa-clock mr-1"></i> Il y a 13 minutes
                                        </span>
                                    </div>
                                </a>
                                
                                <a href="#" class="dropdown-item">
                                    <div class="dropdown-item-icon icon-red">
                                        <i class="fab fa-spotify text-white"></i>
                                    </div>
                                    <div class="dropdown-item-content">
                                        <h4 class="dropdown-item-title">Nouvel album</h4>
                                        <p class="dropdown-item-desc">Travis Scott a sorti un nouvel album</p>
                                        <span class="dropdown-item-time">
                                            <i class="fas fa-clock mr-1"></i> Il y a 1 jour
                                        </span>
                                    </div>
                                </a>
                                
                                <a href="#" class="dropdown-item">
                                    <div class="dropdown-item-icon icon-green">
                                        <i class="fas fa-credit-card text-white"></i>
                                    </div>
                                    <div class="dropdown-item-content">
                                        <h4 class="dropdown-item-title">Paiement complété</h4>
                                        <p class="dropdown-item-desc">Votre paiement a été traité</p>
                                        <span class="dropdown-item-time">
                                            <i class="fas fa-clock mr-1"></i> Il y a 2 jours
                                        </span>
                                    </div>
                                </a>
                                
                                <div class="dropdown-footer">
                                    <a href="#" class="dropdown-view-all">Voir toutes les notifications</a>
                                </div>
                            </div>
                        </div>
                        
                        <div class="user-menu">
                            <img src="https://images.unsplash.com/photo-1535713875002-d1d0cf377fde?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=500&q=80" alt="User" class="user-avatar">
                            <div class="user-info">
                                <span class="user-name">Alexis Thompson</span>
                                <span class="user-role">Administrator</span>
                            </div>
                        </div>
                        
                        <div class="mobile-menu-btn" id="mobile-menu-btn">
                            <span></span>
                            <span></span>
                            <span></span>
                        </div>
                    </div>
                </div>
            </div>
        </nav>
    </div>

    <script>
        // Gestion des dropdowns
        document.addEventListener('DOMContentLoaded', function() {
            const notificationsBtn = document.getElementById('notifications-btn');
            const notificationsDropdown = document.getElementById('notifications-dropdown');
            
            // Toggle notifications dropdown
            notificationsBtn.addEventListener('click', function(e) {
                e.stopPropagation();
                notificationsDropdown.classList.toggle('show');
            });
            
            // Fermer le dropdown en cliquant à l'extérieur
            document.addEventListener('click', function(e) {
                if (!notificationsDropdown.contains(e.target) && e.target !== notificationsBtn) {
                    notificationsDropdown.classList.remove('show');
                }
            });
            
            // Empêcher la fermeture lorsqu'on clique à l'intérieur du dropdown
            notificationsDropdown.addEventListener('click', function(e) {
                e.stopPropagation();
            });
            
            // Animation du bouton menu mobile
            const mobileMenuBtn = document.getElementById('mobile-menu-btn');
            mobileMenuBtn.addEventListener('click', function() {
                this.classList.toggle('active');
            });
        });
    </script>
</body>
</html>